<div class="col-md-3">
        <%= link_to(offer) do %>
          <div class="thumbnail offer" style="background-image: url(<%= asset_path(offer.image) %>)">
            <p><%= offer.description %></p>
          </div>
        <% end %>
</div>
