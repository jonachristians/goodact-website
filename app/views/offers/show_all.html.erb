<div class="container">
  <div class="row">
    <% @offers.each do |offer| %>
      <div class="col-sm-3">
        <%= link_to user_offer_path(offer.user, offer) do %>
          <div class="thumbnail">
            <p><%= offer.description %></p>
            <p>
              Offered by <%= offer.user.name%>
            </p>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
