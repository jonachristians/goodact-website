<% if flash.any? %>
  <div class="flash-messages">
    <% flash.each do |name, message| %>
      <div class="alert alert-<%= name %>">
        <%= message %>
      </div>
    <% end %>
  </div>
<% end %>
